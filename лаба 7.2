#include <iostream>
#include<cmath>
using namespace std;
int main()
{
    float a, r;
    cout<<"Enter an angle value in radians from 0 to 2PI!"<<endl;
    cin>>r;
    if (r > 0 and r < 2*M_PI){
        a =180*r/M_PI;
        cout<<"This angle in degrees a = "<<a<<endl;
    } else {
        cout<<"The angle you entered is not within the specified limits (from 0 to 2PI)!"<<endl;
    }
    return 0;
}
