#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float a, b, c, res;
    cout<<"enter the value of the number A"<<endl;
    cin >>a;
    b = pow(a, 5);
    c = pow(a, 2);
    res = b*b*c*a*a*a;
    
    cout<<"A^15="<<res<<endl;
    
    return 0;
}
